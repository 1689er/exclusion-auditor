# User-owned config may reference local paths / secrets — never commit it.
config.yaml
config.local.yaml

# Audit output — the full report is CONFIDENTIAL; never commit it
*CONFIDENTIAL*
*.full.json
audit.full*
# Persistent token salt enables value correlation — keep private
*.salt
salt.txt

# Secrets / credentials — never commit
.env
*.env
*.pem
*.key
*.p12
*.pfx
id_rsa*
credentials.json
secrets.*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
