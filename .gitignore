# User-owned config may reference local paths / secrets — never commit it.
config.yaml
config.local.yaml

# Secrets / credentials — never commit
.env
*.env
*.pem
*.key
*.p12
*.pfx
id_rsa*
credentials.json
secrets.*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
